CREATE DEFINER=`root`@`localhost` PROCEDURE `procedimiento2`(in nombre varchar(50), in edad int)
BEGIN
	DECLARE n1 INT;
	DECLARE n2 int;
	SET n1 = 21;
	SELECT 
	* 
	FROM
	persona WHERE id=n1;
	
	
	
	SET n2 = 2;
	SET n1 = 25;
END